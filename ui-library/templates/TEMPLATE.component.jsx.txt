/**
 * This is a template file for creating components.
 * DO NOT import this file directly in your code.
 * 
 * To use this template:
 * 1. Copy this file to your component directory
 * 2. Rename it to match your component name (e.g., Button.jsx)
 * 3. Replace all instances of "ComponentName" with your actual component name
 * 4. Uncomment and modify the code as needed
 */

// The commented code below is meant to be used as a template

/*
import React from 'react';
import PropTypes from 'prop-types';
import { cn } from '../../utils/cn';

/**
 * [Component Description]
 * 
 * @component
 * @example
 * ```jsx
 * <ComponentName variant="primary" size="md">Content</ComponentName>
 * ```
 */
const ComponentName = ({
  className,
  children,
  variant = 'default',
  size = 'md',
  ...props
}) => {
  return (
    <div
      className={cn(
        'component-base-class',
        {
          'variant-class': variant === 'default',
          'size-class': size === 'md',
        },
        className
      )}
      {...props}
    >
      {children}
    </div>
  );
};

ComponentName.propTypes = {
  /** Additional class names */
  className: PropTypes.string,
  /** Component content */
  children: PropTypes.node,
  /** Component variant */
  variant: PropTypes.oneOf(['default', 'primary', 'secondary']),
  /** Component size */
  size: PropTypes.oneOf(['sm', 'md', 'lg']),
};

export default ComponentName;
*/
